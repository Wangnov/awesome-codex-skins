# Disclaimer · 免责声明

**Unofficial.** This project is not affiliated with, endorsed by, or sponsored by OpenAI. "Codex" is referenced descriptively to identify the target application. 本项目与 OpenAI 无关，"Codex" 仅为描述性使用。

**No app modification.** Skins never modify, unpack or replace any file of the Codex application. They inject CSS/DOM decoration into the running renderer via the Chrome DevTools Protocol on 127.0.0.1, are fully reversible, and leave the application's code signature intact. 皮肤不改动应用的任何文件，注入完全可逆，签名完好。

**Scope of the MIT license · MIT 协议适用范围.** The repository's [LICENSE](LICENSE) (MIT) covers all code and documentation (`studio/`, `skills/`, `scripts/`, CI, the spec). It does **not** cover the skin packages under `skins/` — their artwork is AI-generated fan content governed by the terms below and each manifest's `license` field. 根 LICENSE（MIT）覆盖全部代码与文档，**不覆盖** `skins/` 下的皮肤素材——其授权以下文与各包 `license` 字段为准。

**Fan art.** Skins in this repository that reference existing IPs (e.g. tokusatsu or anime characters) are non-commercial fan creations, generated with AI image tools, distributed free of charge for personal use only (`"license": "personal-use"`). They must not be sold or used commercially. 引用现有 IP 的皮肤均为非商业同人创作，仅供个人使用，禁止商用。

**Takedown.** If you are a rights holder and believe any content here infringes your rights, open an issue titled `[Takedown] <skin-id>` or contact the maintainer — the content will be removed from the repository **and the distribution CDN** promptly upon verification. 权利人可通过 Issue（标题 `[Takedown] <skin-id>`）或直接联系维护者请求下架，核实后将立即从仓库与分发 CDN 移除。

**No warranty.** Skins are provided "as is". A Codex update may change its DOM and break a skin's styling until the skin is updated (`codexVerified` in each manifest records the version it was verified against). 皮肤按现状提供；Codex 更新可能导致样式失效，以 `codexVerified` 字段标注的验证版本为准。
